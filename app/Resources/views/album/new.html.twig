{% extends 'base.html.twig' %}

{% block body %}
    <h1>Album creation</h1>
    <form action="{{ path('_new') }}" method="post" {{ form_enctype(form) }}>
    {{ form_start(form) }}
    </form>
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('_index') }}">Back to the list</a>
        </li>
    </ul>

{% endblock %}
